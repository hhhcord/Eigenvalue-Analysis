要求に従い、クラスファイルに関数を追加せよ。
追加した関数をかけ。

要求：
    正方行列を受け取り、つぎの変換を行え。
    A = scipy.linalg.logm(A) / self.Ts
    つぎの処理で固有値を求めよ。
    eig = np.linalg.eigvals(A)
    固有値の虚部から固有角振動数を求め、求めたものを固有振動数（Hz）に変換せよ。
    つぎの内容を.csvファイルで記録せよ。
    .csv：
        固有値の実部、固有振動数（Hz）
    .csvには、固有値の実部が大きい順に記録せよ。
    固有振動数（Hz）に該当する固有値の実部を記録せよ。
    共役であるから、固有振動数（Hz）に該当する固有値は、それぞれ二つ存在するはずである。
    処理は、細かく分割して行え。
    複数の関数を作り、一つ、それらの関数を組み合わせた関数を作れ。
    わかりやすい変数名にせよ。
    可読性の良いコードをかけ。
    コメントは、英語でかけ。

クラスファイル：
